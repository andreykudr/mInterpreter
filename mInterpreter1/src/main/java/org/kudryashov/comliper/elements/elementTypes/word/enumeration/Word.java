package org.kudryashov.comliper.elements.elementTypes.word.enumeration;

import org.kudryashov.comliper.elements.elementTypes.ElementName;

public enum Word implements ElementName {

    PROGRAM,
    VAR,
    INT,
    BOOL,
    FLOAT,
    BEGIN,
    END,
    IF,
    THEN,
    ELSE,
    WHILE,
    DO,
    FOR,
    TO,
    READ,
    WRITE,
    TRUE,
    FALSE,
    NOT,
    AS
}
